# Production Environment Variables for nubo.email

# Database Configuration
POSTGRES_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD
DATABASE_URL=postgresql://nubo:CHANGE_THIS_TO_SECURE_PASSWORD@postgres:5432/nubo_email

# Redis Configuration  
REDIS_PASSWORD=CHANGE_THIS_TO_SECURE_PASSWORD

# Security - MUST CHANGE!
JWT_SECRET=CHANGE_THIS_TO_64_CHAR_RANDOM_STRING
ENCRYPTION_KEY=EXACTLY_32_CHARACTERS_CHANGE_ME

# Application URLs (Production with subdomains)
CORS_ORIGIN=https://nubo.email
NEXT_PUBLIC_API_URL=https://api.nubo.email/api

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@nubo.email
SMTP_PASS=your-smtp-app-password

# Domains
DOMAIN=nubo.email
API_DOMAIN=api.nubo.email
SSL_EMAIL=admin@nubo.email

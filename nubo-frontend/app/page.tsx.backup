'use client';

import { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import Link from 'next/link';
import { motion } from 'framer-motion';
import { 
  Mail, Shield, Users, Globe, Check, Star, ArrowRight, 
  ChevronRight, Award, Building, IndianRupee, Zap, Lock,
  Database, Cloud, Phone, MessageCircle, Headphones,
  TrendingDown, Server, FileText, DollarSign
} from 'lucide-react';
import { Button } from '@/components/ui/button';

const fadeIn = {
  hidden: { opacity: 0, y: 20 },
  visible: { opacity: 1, y: 0 }
};

export default function LandingPage() {
  const router = useRouter();
  const [selectedPlan, setSelectedPlan] = useState('unlimited');

  const unlimitedPlans = [
    { space: '5 GB', price: '₹1,820', popular: false },
    { space: '25 GB', price: '₹6,370', popular: true },
    { space: '100 GB', price: '₹21,190', popular: false },
    { space: '500 GB', price: '₹83,200', popular: false },
  ];

  const perUserPlans = [
    { space: '1 GB', price: '₹247', perUser: true },
    { space: '10 GB', price: '₹676', perUser: true },
    { space: '50 GB', price: '₹1,235', perUser: true },
    { space: '100 GB', price: '₹2,054', perUser: true },
  ];

  const trustedBy = [
    'Ujjivan Bank', 'Jockey', 'Kurlon', 
    'Eastern Condiments', 'Suryoday Bank'
  ];

  return (
    <div className="min-h-screen bg-gradient-to-b from-white to-gray-50">
      {/* Navigation */}
      <nav className="sticky top-0 z-50 bg-white/95 backdrop-blur-sm border-b">
        <div className="container mx-auto px-4 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-2">
              <div className="w-10 h-10 bg-blue-600 rounded-lg flex items-center justify-center">
                <Mail className="w-6 h-6 text-white" />
              </div>
              <span className="text-2xl font-bold">Nubo.email</span>
            </div>
            <div className="hidden md:flex items-center space-x-8">
              <Link href="#features" className="text-gray-600 hover:text-blue-600">Features</Link>
              <Link href="#pricing" className="text-gray-600 hover:text-blue-600">Pricing</Link>
              <Link href="#clients" className="text-gray-600 hover:text-blue-600">Clients</Link>
              <Link href="/login" className="text-gray-600 hover:text-blue-600">Login</Link>
              <Button onClick={() => router.push('/signup')} className="bg-blue-600 hover:bg-blue-700">
                Get Started Free
              </Button>
            </div>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="pt-20 pb-32 px-4">
        <div className="container mx-auto max-w-6xl">
          <motion.div 
            initial="hidden"
            animate="visible"
            variants={fadeIn}
            className="text-center"
          >
            <div className="inline-flex items-center px-4 py-2 bg-orange-100 text-orange-800 rounded-full mb-6">
              <span className="text-sm font-semibold">🇮🇳 Made in India • Data Stored in India</span>
            </div>
            
            <h1 className="text-5xl md:text-7xl font-bold text-gray-900 mb-6">
              Cut Your Email Costs by
              <span className="text-blue-600"> 70-90%</span>
            </h1>
            
            <p className="text-xl text-gray-600 mb-8 max-w-3xl mx-auto">
              The economical, secure, and scalable alternative to GSuite & Office365. 
              <strong> Unlimited users</strong>, hybrid options, and enterprise-grade compliance — 
              trusted by 1000s of Indian businesses.
            </p>

            <div className="flex flex-col sm:flex-row gap-4 justify-center mb-12">
              <Button 
                size="lg" 
                className="bg-blue-600 hover:bg-blue-700 text-lg px-8 py-6"
                onClick={() => router.push('/signup')}
              >
                Start for Just ₹1,820/Year
                <ArrowRight className="ml-2 w-5 h-5" />
              </Button>
              <Button 
                size="lg" 
                variant="outline"
                className="text-lg px-8 py-6"
                onClick={() => document.getElementById('pricing')?.scrollIntoView()}
              >
                Compare Plans
              </Button>
            </div>

            {/* Trust Badges */}
            <div className="flex flex-wrap justify-center gap-8 items-center">
              <div className="flex items-center space-x-2">
                <Shield className="w-5 h-5 text-green-600" />
                <span className="text-sm text-gray-600">Bank-Grade Security</span>
              </div>
              <div className="flex items-center space-x-2">
                <Award className="w-5 h-5 text-green-600" />
                <span className="text-sm text-gray-600">96% Retention Rate</span>
              </div>
              <div className="flex items-center space-x-2">
                <Users className="w-5 h-5 text-green-600" />
                <span className="text-sm text-gray-600">1000+ Domains Hosted</span>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Cost Comparison */}
      <section className="py-20 bg-blue-50">
        <div className="container mx-auto max-w-6xl px-4">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
          >
            <h2 className="text-4xl font-bold text-center mb-12">
              Save Up to <span className="text-green-600">₹1.2 Crore</span> Per Year
            </h2>
            
            <div className="grid md:grid-cols-3 gap-8">
              <div className="bg-white rounded-xl p-6 shadow-lg">
                <h3 className="text-xl font-semibold mb-4">GSuite Business</h3>
                <div className="text-3xl font-bold text-red-600 mb-2">₹1,36,800</div>
                <p className="text-gray-600 mb-4">Per year for 100 users</p>
                <ul className="space-y-2 text-gray-600">
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    Charged per user
                  </li>
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    Limited storage per user
                  </li>
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    No hybrid options
                  </li>
                </ul>
              </div>

              <div className="bg-gradient-to-br from-blue-600 to-blue-700 rounded-xl p-6 shadow-xl text-white transform scale-105">
                <div className="bg-green-500 text-white text-xs font-bold px-3 py-1 rounded-full inline-block mb-4">
                  RECOMMENDED
                </div>
                <h3 className="text-xl font-semibold mb-4">Nubo.email</h3>
                <div className="text-3xl font-bold mb-2">₹21,190</div>
                <p className="text-blue-100 mb-4">Per year for unlimited users</p>
                <ul className="space-y-2">
                  <li className="flex items-start">
                    <Check className="w-5 h-5 text-green-400 mr-2 flex-shrink-0" />
                    Unlimited users
                  </li>
                  <li className="flex items-start">
                    <Check className="w-5 h-5 text-green-400 mr-2 flex-shrink-0" />
                    100 GB pooled storage
                  </li>
                  <li className="flex items-start">
                    <Check className="w-5 h-5 text-green-400 mr-2 flex-shrink-0" />
                    Hybrid deployment
                  </li>
                </ul>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg">
                <h3 className="text-xl font-semibold mb-4">Office 365</h3>
                <div className="text-3xl font-bold text-red-600 mb-2">₹1,44,000</div>
                <p className="text-gray-600 mb-4">Per year for 100 users</p>
                <ul className="space-y-2 text-gray-600">
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    Charged per user
                  </li>
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    Complex licensing
                  </li>
                  <li className="flex items-start">
                    <span className="text-red-500 mr-2">✗</span>
                    Foreign data storage
                  </li>
                </ul>
              </div>
            </div>

            <div className="text-center mt-8">
              <p className="text-2xl font-semibold text-green-600">
                You Save: ₹1,15,610 - ₹1,22,810 per year!
              </p>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Features */}
      <section id="features" className="py-20">
        <div className="container mx-auto max-w-6xl px-4">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
          >
            <h2 className="text-4xl font-bold text-center mb-4">
              Enterprise Features at SME Prices
            </h2>
            <p className="text-xl text-gray-600 text-center mb-12">
              Everything you need to run professional email, nothing you don't
            </p>

            <div className="grid md:grid-cols-3 gap-8">
              <div className="text-center">
                <div className="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Users className="w-8 h-8 text-blue-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">Unlimited Users</h3>
                <p className="text-gray-600">
                  Add unlimited email accounts without per-user charges. Perfect for growing teams.
                </p>
              </div>

              <div className="text-center">
                <div className="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Shield className="w-8 h-8 text-green-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">Bank-Grade Security</h3>
                <p className="text-gray-600">
                  Pass regulatory audits with enterprise security, spam filtering, and access controls.
                </p>
              </div>

              <div className="text-center">
                <div className="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Zap className="w-8 h-8 text-purple-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">Hybrid Deployment</h3>
                <p className="text-gray-600">
                  Keep critical emails on GSuite/O365 while moving others to Nubo. Best of both worlds.
                </p>
              </div>

              <div className="text-center">
                <div className="w-16 h-16 bg-orange-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Server className="w-8 h-8 text-orange-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">Made in India</h3>
                <p className="text-gray-600">
                  Your data stays in India. Compliant with local regulations and data sovereignty laws.
                </p>
              </div>

              <div className="text-center">
                <div className="w-16 h-16 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Database className="w-8 h-8 text-red-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">Email Archival</h3>
                <p className="text-gray-600">
                  Store years of emails securely. Retrieve old emails instantly for audits and compliance.
                </p>
              </div>

              <div className="text-center">
                <div className="w-16 h-16 bg-indigo-100 rounded-full flex items-center justify-center mx-auto mb-4">
                  <Headphones className="w-8 h-8 text-indigo-600" />
                </div>
                <h3 className="text-xl font-semibold mb-2">24/7 Support</h3>
                <p className="text-gray-600">
                  Phone, chat, and email support. No partner liability - we handle everything directly.
                </p>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Pricing */}
      <section id="pricing" className="py-20 bg-gray-50">
        <div className="container mx-auto max-w-6xl px-4">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
          >
            <h2 className="text-4xl font-bold text-center mb-4">
              Simple, Transparent Pricing
            </h2>
            <p className="text-xl text-gray-600 text-center mb-8">
              No hidden fees. No per-user charges. Just honest pricing.
            </p>

            {/* Plan Toggle */}
            <div className="flex justify-center mb-12">
              <div className="bg-white rounded-lg p-1 shadow-md">
                <button
                  onClick={() => setSelectedPlan('unlimited')}
                  className={`px-6 py-3 rounded-lg font-semibold transition-all ${
                    selectedPlan === 'unlimited' 
                      ? 'bg-blue-600 text-white' 
                      : 'text-gray-600 hover:text-gray-900'
                  }`}
                >
                  Unlimited Users (Recommended)
                </button>
                <button
                  onClick={() => setSelectedPlan('peruser')}
                  className={`px-6 py-3 rounded-lg font-semibold transition-all ${
                    selectedPlan === 'peruser' 
                      ? 'bg-blue-600 text-white' 
                      : 'text-gray-600 hover:text-gray-900'
                  }`}
                >
                  Per User Plans
                </button>
              </div>
            </div>

            {/* Pricing Cards */}
            <div className="grid md:grid-cols-4 gap-6">
              {(selectedPlan === 'unlimited' ? unlimitedPlans : perUserPlans).map((plan, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, y: 20 }}
                  animate={{ opacity: 1, y: 0 }}
                  transition={{ delay: index * 0.1 }}
                  className={`bg-white rounded-xl shadow-lg overflow-hidden ${
                    plan.popular ? 'ring-2 ring-blue-600 transform scale-105' : ''
                  }`}
                >
                  {plan.popular && (
                    <div className="bg-blue-600 text-white text-center py-2 text-sm font-semibold">
                      MOST POPULAR
                    </div>
                  )}
                  <div className="p-6">
                    <h3 className="text-2xl font-bold mb-2">{plan.space}</h3>
                    <div className="text-3xl font-bold text-blue-600 mb-1">
                      {plan.price}
                    </div>
                    <p className="text-gray-600 mb-6">
                      {plan.perUser ? 'per user/year' : 'per year'}
                    </p>
                    <ul className="space-y-3 mb-6">
                      <li className="flex items-start">
                        <Check className="w-5 h-5 text-green-500 mr-2 flex-shrink-0" />
                        <span className="text-sm">
                          {selectedPlan === 'unlimited' ? 'Unlimited email accounts' : 'Pooled storage'}
                        </span>
                      </li>
                      <li className="flex items-start">
                        <Check className="w-5 h-5 text-green-500 mr-2 flex-shrink-0" />
                        <span className="text-sm">Multiple domains</span>
                      </li>
                      <li className="flex items-start">
                        <Check className="w-5 h-5 text-green-500 mr-2 flex-shrink-0" />
                        <span className="text-sm">24/7 support</span>
                      </li>
                      <li className="flex items-start">
                        <Check className="w-5 h-5 text-green-500 mr-2 flex-shrink-0" />
                        <span className="text-sm">99.9% uptime SLA</span>
                      </li>
                    </ul>
                    <Button 
                      className={`w-full ${plan.popular ? 'bg-blue-600 hover:bg-blue-700' : ''}`}
                      variant={plan.popular ? 'default' : 'outline'}
                      onClick={() => router.push(`/signup?plan=${plan.space.toLowerCase().replace(' ', '')}`)}
                    >
                      Get Started
                    </Button>
                  </div>
                </motion.div>
              ))}
            </div>

            {/* Free Personal Email */}
            <div className="mt-12 bg-gradient-to-r from-purple-600 to-blue-600 rounded-2xl p-8 text-white">
              <div className="max-w-3xl mx-auto text-center">
                <h3 className="text-2xl font-bold mb-4">
                  Free @nubo.email for Personal Use!
                </h3>
                <p className="text-lg mb-6">
                  Get your professional email address with 5GB storage absolutely FREE. 
                  Perfect for individuals and freelancers.
                </p>
                <Button 
                  size="lg" 
                  className="bg-white text-blue-600 hover:bg-gray-100"
                  onClick={() => router.push('/signup?plan=free')}
                >
                  Claim Your Free Email
                  <ArrowRight className="ml-2 w-5 h-5" />
                </Button>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Industry Solutions */}
      <section className="py-20">
        <div className="container mx-auto max-w-6xl px-4">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
          >
            <h2 className="text-4xl font-bold text-center mb-12">
              Trusted Across Industries
            </h2>

            <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
              <div className="bg-white rounded-xl p-6 shadow-lg border-t-4 border-blue-600">
                <Building className="w-12 h-12 text-blue-600 mb-4" />
                <h3 className="text-xl font-semibold mb-2">Banks & Finance</h3>
                <p className="text-gray-600">
                  Pass audits, store all emails securely, comply with RBI regulations.
                </p>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg border-t-4 border-green-600">
                <FileText className="w-12 h-12 text-green-600 mb-4" />
                <h3 className="text-xl font-semibold mb-2">CA & Legal Firms</h3>
                <p className="text-gray-600">
                  Restrict staff access, store client files, maintain confidentiality.
                </p>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg border-t-4 border-purple-600">
                <Globe className="w-12 h-12 text-purple-600 mb-4" />
                <h3 className="text-xl font-semibold mb-2">Manufacturers</h3>
                <p className="text-gray-600">
                  Reliable communication, multi-device access, supply chain integration.
                </p>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg border-t-4 border-orange-600">
                <TrendingDown className="w-12 h-12 text-orange-600 mb-4" />
                <h3 className="text-xl font-semibold mb-2">Growing SMEs</h3>
                <p className="text-gray-600">
                  Unlimited users, predictable costs, scale without budget worries.
                </p>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* Social Proof */}
      <section id="clients" className="py-20 bg-blue-50">
        <div className="container mx-auto max-w-6xl px-4">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
          >
            <h2 className="text-4xl font-bold text-center mb-4">
              Trusted by 1000+ Indian Businesses
            </h2>
            <p className="text-xl text-gray-600 text-center mb-12">
              Join companies that have already cut their email costs
            </p>

            <div className="flex flex-wrap justify-center gap-12 items-center mb-12">
              {trustedBy.map((company, index) => (
                <motion.div
                  key={index}
                  initial={{ opacity: 0, scale: 0.8 }}
                  animate={{ opacity: 1, scale: 1 }}
                  transition={{ delay: index * 0.1 }}
                  className="text-2xl font-semibold text-gray-700"
                >
                  {company}
                </motion.div>
              ))}
            </div>

            {/* Testimonials */}
            <div className="grid md:grid-cols-3 gap-8">
              <div className="bg-white rounded-xl p-6 shadow-lg">
                <div className="flex mb-4">
                  {[...Array(5)].map((_, i) => (
                    <Star key={i} className="w-5 h-5 text-yellow-500 fill-current" />
                  ))}
                </div>
                <p className="text-gray-600 mb-4">
                  "Switched from GSuite and saved ₹8 lakhs annually. The hybrid setup lets us keep critical emails on GSuite while saving costs on others."
                </p>
                <div className="font-semibold">Manufacturing Company</div>
                <div className="text-sm text-gray-500">500+ employees</div>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg">
                <div className="flex mb-4">
                  {[...Array(5)].map((_, i) => (
                    <Star key={i} className="w-5 h-5 text-yellow-500 fill-current" />
                  ))}
                </div>
                <p className="text-gray-600 mb-4">
                  "Bank audit compliant, secure, and 70% cheaper than our previous solution. Support team is always available when needed."
                </p>
                <div className="font-semibold">Regional Bank</div>
                <div className="text-sm text-gray-500">200+ employees</div>
              </div>

              <div className="bg-white rounded-xl p-6 shadow-lg">
                <div className="flex mb-4">
                  {[...Array(5)].map((_, i) => (
                    <Star key={i} className="w-5 h-5 text-yellow-500 fill-current" />
                  ))}
                </div>
                <p className="text-gray-600 mb-4">
                  "Perfect for our CA firm. Client data stays in India, access controls work great, and we can add unlimited junior staff."
                </p>
                <div className="font-semibold">CA Firm</div>
                <div className="text-sm text-gray-500">50+ employees</div>
              </div>
            </div>
          </motion.div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 bg-gradient-to-r from-blue-600 to-blue-700">
        <div className="container mx-auto max-w-4xl px-4 text-center">
          <motion.div
            initial="hidden"
            whileInView="visible"
            viewport={{ once: true }}
            variants={fadeIn}
            className="text-white"
          >
            <h2 className="text-4xl font-bold mb-4">
              Stop Overpaying for Email
            </h2>
            <p className="text-xl mb-8 text-blue-100">
              Join 1000+ businesses saving lakhs on email every year
            </p>
            
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Button 
                size="lg" 
                className="bg-white text-blue-600 hover:bg-gray-100 text-lg px-8 py-6"
                onClick={() => router.push('/signup')}
              >
                Start 30-Day Free Trial
                <ArrowRight className="ml-2 w-5 h-5" />
              </Button>
              <Button 
                size="lg" 
                variant="outline"
                className="bg-transparent text-white border-white hover:bg-white hover:text-blue-600 text-lg px-8 py-6"
                onClick={() => router.push('/demo')}
              >
                Book a Demo
                <Phone className="ml-2 w-5 h-5" />
              </Button>
            </div>

            <p className="mt-6 text-blue-100">
              No credit card required • Setup in 5 minutes • Cancel anytime
            </p>
          </motion.div>
        </div>
      </section>

      {/* Footer */}
      <footer className="bg-gray-900 text-gray-300 py-12">
        <div className="container mx-auto max-w-6xl px-4">
          <div className="grid md:grid-cols-4 gap-8 mb-8">
            <div>
              <div className="flex items-center space-x-2 mb-4">
                <div className="w-8 h-8 bg-blue-600 rounded-lg flex items-center justify-center">
                  <Mail className="w-5 h-5 text-white" />
                </div>
                <span className="text-xl font-bold text-white">Nubo.email</span>
              </div>
              <p className="text-sm">
                India's most affordable business email solution. 
                Made in India, for India.
              </p>
            </div>

            <div>
              <h4 className="font-semibold text-white mb-4">Product</h4>
              <ul className="space-y-2 text-sm">
                <li><Link href="/features" className="hover:text-white">Features</Link></li>
                <li><Link href="/pricing" className="hover:text-white">Pricing</Link></li>
                <li><Link href="/security" className="hover:text-white">Security</Link></li>
                <li><Link href="/compliance" className="hover:text-white">Compliance</Link></li>
              </ul>
            </div>

            <div>
              <h4 className="font-semibold text-white mb-4">Company</h4>
              <ul className="space-y-2 text-sm">
                <li><Link href="/about" className="hover:text-white">About Us</Link></li>
                <li><Link href="/contact" className="hover:text-white">Contact</Link></li>
                <li><Link href="/support" className="hover:text-white">Support</Link></li>
                <li><Link href="/blog" className="hover:text-white">Blog</Link></li>
              </ul>
            </div>

            <div>
              <h4 className="font-semibold text-white mb-4">Contact</h4>
              <ul className="space-y-2 text-sm">
                <li className="flex items-center">
                  <Phone className="w-4 h-4 mr-2" />
                  +91 98765 43210
                </li>
                <li className="flex items-center">
                  <Mail className="w-4 h-4 mr-2" />
                  sales@nubo.email
                </li>
                <li className="flex items-center">
                  <MessageCircle className="w-4 h-4 mr-2" />
                  Live Chat Support
                </li>
              </ul>
            </div>
          </div>

          <div className="border-t border-gray-800 pt-8 text-center text-sm">
            <p>© 2024 Chandorkar Technologies. All rights reserved.</p>
            <p className="mt-2">
              <Link href="/privacy" className="hover:text-white">Privacy Policy</Link>
              {' • '}
              <Link href="/terms" className="hover:text-white">Terms of Service</Link>
              {' • '}
              <Link href="/refund" className="hover:text-white">Refund Policy</Link>
            </p>
          </div>
        </div>
      </footer>
    </div>
  );
}